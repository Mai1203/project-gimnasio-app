'use client';

import { Sidebar } from '../ui/Sidebar';

/**
 * Admin sidebar wrapper component
 * Can be extended with admin-specific functionality
 */
export function AdminSidebar() {
  return <Sidebar />;
}